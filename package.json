{
  "name": "gerenciahub-portfolio",
  "version": "3.0.0",
  "private": true,
  "description": "Central de projetos com GerenciaHub, visualizador de código e publicação no GitHub/Vercel.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check public/gerenciahub/app.js && node --check public/script.js && node --check public/storage.js && node --check public/editor.js && node --check public/project.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
