{
  "name": "github-file-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Gerenciador web responsivo de arquivos e repositórios do GitHub.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
