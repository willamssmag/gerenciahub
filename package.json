{
  "name": "gerenciahub-portfolio",
  "version": "4.0.0",
  "private": true,
  "description": "Central de projetos com cada aplicativo isolado em sua própria pasta dentro de public.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check public/gerenciahub/app.js && node --check public/portfolio/script.js && node --check public/portfolio/storage.js && node --check public/visualizador/editor.js && node --check public/visualizador/storage.js && node --check public/visualizador/project.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
